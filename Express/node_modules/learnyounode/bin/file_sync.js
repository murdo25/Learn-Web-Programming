var fs = require('fs');

var filename = process.argv[2];

file = fs.readFile(filename, function(err, data) {
    console.log(data.toString().split('\n').length - 1);
});

// var fs = require('fs')

// var buff = fs.readFileSync(process.argv[2], 'utf8')

// // console.log(String(buff))

// console.log(String(buff).split(/\r\n|\r|\n/).length - 1)
